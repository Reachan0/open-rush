# AO-04 固定服务端到端验收

验收环境：`web-e2e-20260909T163244414Z`，服务端口 `8766/3100/18787/18081`。
数据库和控制器数据留在该输出目录中，凭据不写入本文件。

## 进程退出

- 控制台先把真实受管进程 `51428` 退出。
- OpenRush 新 Run：`740e70e8-ec5a-4b9f-83dc-e71098dd5952`。
- 控制器事件：`incident.detected → incident.action → 3 × incident.verify → incident.terminal`。
- 旧代次为 3，新代次为 4，PID 变为 `51504`；三次验证的健康、业务、归属均为 `true`。
- OpenRush DAG：`pre=completed`、`protected=completed`、`post=completed`，页面显示 `3/3`。

## 持续超时

- 控制台把真实 `/work` 接口切换为持续超时，进程 PID `50710` 保持运行。
- OpenRush Run：`30984544-9ab5-4cf8-9dae-8989e8a0057b`。
- 控制器终态：`incident.terminal(status=degraded, attempts=3)`，实例保持 `locked=true`。
- OpenRush DAG：`pre=completed`、`protected=failed`、`post=skipped`，不是停留在 pending。
- 下一 Run `88956738-2278-426c-8a95-4933c04ab3ba` 返回 `circuit_open`，没有新增依赖尝试；`post` 仍为 skipped。

## 代码级验证

- `@open-rush/workflow`：104 tests，12 files；TypeScript check 通过。
- `@open-rush/dsh-tool-workflow-run`：44 tests，5 files；TypeScript check 通过。
- `@open-rush/dsh-tool-ao04`：8 tests，2 files；TypeScript check 通过。
- `@open-rush/agent-worker`：67 tests，6 files。
- `@open-rush/control-worker`：3 tests，1 file。
- AO-04 控制器相关回归：64 tests；固定控制台代理：5 tests。

这份验收只证明固定服务 demo 的真实链路，不把 M3 说成 OpenRush 的全局工具保护；没有受保护工具的普通工作流不会自动获得 AO-04 处置能力。
