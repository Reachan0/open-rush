# AO-04 M3 本地演示验收记录（2026-09-09）

结论：快车道调用 AO-04 受保护工具、真实进程恢复、超时阻断后续节点、聊天 DAG 与可靠性事件回看均有实测。可作为本地课题演示；不宣称任意自然语言稳定触发或生产级可靠性。

## 本轮代码与验证

- 修复可靠性事件与聊天事件争用 seq；DAG 按实际节点终态显示。
- 区分规划 JSON 与结果撰写；规划调用默认 45 秒超时并传递取消。
- DSH 请求级超时、初始化失败回收、初始化期间取消后不得 prompt；通知阶段也有空闲超时。
- 运行时致命错误使 Run 失败，未完成工具卡片结束为错误；已取消 Run 不再走成功最终化。
- 受保护图任何失败（含执行器超时）均禁止外层修复提示绕过依赖；普通非受保护图保留原行为。
- 全量 20 个包类型检查通过。相关包测试：agent-runtime 74、control-plane 591、workflow 104、dsh-tool-workflow-run 44，共 813。前三个受本轮复审修改的包构建通过。
- 6 个确定性场景：`results/final-20260909T064553Z`；当前源码复验 PASS。该批次为 deterministic，不冒充真实 DSH 批次。

## 真实页面证据

路径前缀：`results/web-e2e-20260909T035021831Z/evidence/`。

| 用例 | 目录 / Run | 观察结果 |
| --- | --- | --- |
| 最终代码自然语言恢复 | final-code-exit / 34e78f1a-029a-467a-b246-05217ec47b34 | workflow_run 只有 intent、无 DSL；PID 22577→22715；连续 3 次健康+业务+归属通过；DAG 3/3；刷新仍在 |
| 自然语言超时降级 | natural-timeout-final / 822c3197-d1ac-4276-9b5d-8bce5ddb8df5 | 3 次后 degraded/locked；DAG 1/4；没有读取 post，也没有外层绕过 |
| 处置未结束时页面取消 | fixed-cancel-live / 664d472e-ae9f-4020-843f-9ff64c92d729 | Python cancelled=true；事件停在 8 条 incident.detected，无三次终态；Run failed |
| 取消后新任务 | after-cancel-healthy / 634aa1fa-f589-48e6-affb-207173ab00c1 | 固定 DSL 3/3 完成；旧 Run 迟到 abort 返回 run mismatch，新实验未被取消 |

取消用例为扩大观察窗口使用 dependency timeout=60s、HANG_SECONDS=180。快车道工具自身先超时，随后在 Python 仍处置时点取消，不能宣称在快车道节点仍运行的瞬间取消。该用例还曾因 read 的 path/file_path 字段不匹配失败并被模型修正，原始记录完整保留。此过程发现的普通失败提示问题已经修复，并有反例测试。

自然语言规划存在失败样本（natural-exit、page-cancel-60）；不从演示样本计算成功率。取消和降级页面证据产生于最后请求级超时/取消竞态补丁之前；补丁有专项回归，最终代码重新跑了自然语言恢复。旧 r3/p2/80 次批次不作为本轮新代码成绩。

## 本地运行与交接

浏览器 `http://127.0.0.1:3100`，DSH worker 18787、AO04 控制面 18081；依赖超时已恢复 4 秒。开发登录 AUTH_SKIP_LOGIN，仅本地。

本次恢复服务时原 Postgres 已停止。为避免写项目外数据库，复制了 PostgreSQL 17 数据到本次 results 下 `postgres17-resumed`，在 127.0.0.1:5432 启动，禁用 Unix socket。原目录未修改。恢复脚本需带 `PGHOST=127.0.0.1`；不要同时启动另一个占用 5432 的数据库。

`web-e2e-current.json` 标识 source/runtime/output；运行的是隔离 runtime，不能只改源 worktree 就认为页面生效。修改后同步对应源码、构建包，再重启自己的 worker。

未 commit/push。没有执行删除命令。不要整体压缩 web-e2e results：包含 runtime、数据库及 private-control.json；交付应仅白名单选取源码、报告、脱敏证据。

## 子 agent 使用

本机 Claude Code：一名只读审查、两名分别限于 packages/agent-runtime 与 packages/control-plane 修复。禁用 Bash，禁止删除和目录外修改。沿用用户模型配置，未独立核实服务端 max 强度。根 agent 运行测试、修正子 agent 的类型/mock 问题、补受保护图超时规则，并进行真实浏览器验收。
